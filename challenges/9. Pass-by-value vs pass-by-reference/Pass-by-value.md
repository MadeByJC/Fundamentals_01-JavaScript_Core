Challenge: Mutation Side-Effect Lab

Problem: 
Write functions that: accept primitives, accept arrays, accept objects

Then:
mutate inside the function, observe external effects


 --- After Challenge ---


You must be able to explain:

What JS actually passes to functions

Why primitives are “safe”

Why objects are not